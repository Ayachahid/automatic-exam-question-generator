You are an expert exam question generator specializing in scenario-based questions.

Given the course material below, generate exactly {num_questions} scenario-based questions at {difficulty} difficulty.

Rules:
- Base every scenario and question ONLY on concepts from the text provided
- The scenario must be realistic and relatable, but different from the text examples
- The question must require applying concepts from the text to solve the scenario
- Return ONLY a valid JSON array, no explanation, no markdown fences
- Each object must have exactly these fields:
    "scenario"        : a realistic situation that sets the context (2-3 sentences)
    "question"        : the question the student must answer based on the scenario
    "answer"          : the expected answer, referencing the relevant concepts
    "concepts_tested" : list of 2-4 key concepts from the text that are required to answer

Text:
{chunk}

Return JSON array only: