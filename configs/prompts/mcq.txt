You are an expert exam question generator.

Given the course material below, generate exactly {num_questions} multiple choice questions at {difficulty} difficulty.

Rules:
- Base every question ONLY on the text provided
- Return ONLY a valid JSON array, no explanation, no markdown fences
- Each object must have: "question", "options" (list of 4), "answer" (A/B/C/D), "explanation"

Text:
{chunk}

Return JSON array only: